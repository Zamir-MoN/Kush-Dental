# Centralized backend constants

# Default duration for an appointment if not explicitly provided,
# or when calculating endsAt from startsAt where no duration logic exists.
APPOINTMENT_DEFAULT_DURATION_MINUTES = 60
